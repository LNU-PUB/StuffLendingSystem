package com.model;

/**
 * Validates member information.
 */
public class MemberValidator {
  /**
   * Validates the input.
   *
   * @param command - The command to use for validation.
   * @param input - The input to validate.
   * @return - True if the input is valid, false otherwise.
   */
  // public boolean validate(ValidationCommand command, String input) {
  //   return command.validate(input);
  // }
}
